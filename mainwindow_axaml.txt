<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="ClinicCore.MainWindow"
        Title="ClinicCore — Hospital Records"
        Width="1100" Height="700"
        WindowStartupLocation="CenterScreen">

    <Window.Styles>
        <Style Selector="Button.navBtn">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#8892B0"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="16,11"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style Selector="Button.navBtn:pointerover /template/ ContentPresenter">
            <Setter Property="Background" Value="#252B45"/>
            <Setter Property="TextElement.Foreground" Value="#CDD6F4"/>
        </Style>
        <Style Selector="Button.navBtn.active /template/ ContentPresenter">
            <Setter Property="Background" Value="#3B82F6"/>
            <Setter Property="TextElement.Foreground" Value="White"/>
        </Style>
    </Window.Styles>

    <Grid ColumnDefinitions="230,*">

        <!-- SIDEBAR -->
        <Border Grid.Column="0" Background="#1A1F36">
            <StackPanel>
                <Border Background="#12172B" Padding="20,26">
                    <StackPanel Spacing="3">
                        <TextBlock Text="ClinicCore"
                                   Foreground="White" FontSize="20" FontWeight="Bold"/>
                        <TextBlock Text="Hospital Records System"
                                   Foreground="#4A5568" FontSize="11"/>
                    </StackPanel>
                </Border>

                <TextBlock Text="MENU" Foreground="#4A5568" FontSize="11"
                           FontWeight="SemiBold" Margin="20,20,0,8"/>

                <StackPanel Name="NavPanel" Margin="10,0" Spacing="2">
                    <Button Classes="navBtn" Tag="Dashboard" Click="Nav_Click">Dashboard</Button>
                    <Button Classes="navBtn active" Tag="Patients" Click="Nav_Click">Patients</Button>
                    <Button Classes="navBtn" Tag="Doctors" Click="Nav_Click">Doctors</Button>
                    <Button Classes="navBtn" Tag="Appointments" Click="Nav_Click">Appointments</Button>
                    <Button Classes="navBtn" Tag="Prescriptions" Click="Nav_Click">Prescriptions</Button>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- CONTENT AREA -->
        <ContentControl Name="MainContent" Grid.Column="1" Background="#F0F4F8"/>

    </Grid>
</Window>
